# ========================================
# Commerce ERP 환경 설정 예시 파일
# ========================================
# 이 파일을 .env로 복사하고 실제 값으로 수정하세요
# cp .env.example .env

# 데이터베이스 설정
DATABASE_HOST=localhost
DATABASE_PORT=3306
DATABASE_USERNAME=your_db_username
DATABASE_PASSWORD=your_db_password
DATABASE_NAME=commerce_erp

# JWT 설정 (32자 이상의 랜덤 문자열 권장)
JWT_SECRET=your_jwt_secret_key_here_32_chars_minimum
JWT_EXPIRES_IN=24h

# 암호화 설정 (32자 정확히 필요)
ENCRYPTION_KEY=your_32_character_encryption_key

# 파일 업로드 설정
FILE_UPLOAD_MAX_SIZE=10485760
ALLOWED_FILE_EXTENSIONS=csv,json

# 서버 설정
PORT=3000
NODE_ENV=development

# 로깅 설정
LOG_LEVEL=info

# ========================================
# 초기 관리자 계정 설정
# ========================================
# 애플리케이션 최초 실행 시 자동으로 생성되는 관리자 계정 설정
# 개발 환경(NODE_ENV=development) 또는 ENABLE_SEEDER=true일 때만 실행됨

# 관리자 사용자명 (필수)
# 규칙: 3-50자, 영문/숫자/언더스코어만 허용, 중복 불가
# 예시: admin, administrator, system_admin
ADMIN_USERNAME=admin

# 관리자 비밀번호 (필수)
# 규칙: 최소 8자 이상, 영문 대소문자/숫자/특수문자 조합 권장
# 보안을 위해 복잡한 비밀번호 사용 필수
# 예시: MySecureP@ssw0rd!, Admin#2024$Strong
ADMIN_PASSWORD=change_this_password_immediately

# 관리자 소속 회사 ID (선택사항)
# 규칙: 3-50자, 영문/숫자/언더스코어만 허용, 고유값
# 기본값: admin_company
ADMIN_COMPANY_ID=admin_company

# 관리자 소속 회사명 (선택사항)
# 규칙: 1-100자, 한글/영문/숫자/공백 허용
# 기본값: Admin Company
ADMIN_COMPANY_NAME=Admin Company

# Seeder 실행 제어 (선택사항)
# true: 프로덕션 환경에서도 seeder 실행
# false 또는 미설정: 개발 환경에서만 실행
# ENABLE_SEEDER=false

# ========================================
# 보안 주의사항
# ========================================
# 1. 프로덕션 환경에서는 반드시 강력한 비밀번호로 변경하세요
# 2. 최초 로그인 후 즉시 비밀번호를 변경하세요
# 3. 이 파일(.env)을 버전 관리 시스템에 커밋하지 마세요
# 4. 서버 배포 시 환경변수로 설정하는 것을 권장합니다
# 5. JWT_SECRET과 ENCRYPTION_KEY는 충분히 복잡하고 예측 불가능한 값을 사용하세요

# ========================================
# 비밀번호 강도 가이드라인
# ========================================
# 강함: MySecureP@ssw0rd2024!, Admin#Strong$Key
# 보통: AdminPass123!, MyPassword2024
# 약함: admin123, password (사용 금지)
#
# 권장 조합:
# - 최소 12자 이상
# - 대문자, 소문자, 숫자, 특수문자 각각 1개 이상
# - 사전에 있는 단어 사용 금지
# - 개인정보(이름, 생일 등) 사용 금지